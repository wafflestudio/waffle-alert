spring:
  config:
    activate:
      on-profile: prod

oci:
  auth:
    type: instance_principal

alert:
  oci-monitoring:
    polling-interval-ms: 180000
    query-window: 4m
    mysql:
      db-systems:
        wafflestudio-mysql:
          thresholds:
            cpu-utilization:
              warning: 80.0
              critical: 90.0
            memory-utilization:
              warning: 80.0
              critical: 90.0
            db-volume-utilization:
              warning: 80.0
              critical: 90.0
